"""Graph nodes (07_LANGGRAPH_WORKFLOW_SPEC.md section 4).

Grouped by stage rather than one file per node: the stages share helpers and a
file holding three ten-line functions is harder to follow than one holding the
retrieval stage.
"""
